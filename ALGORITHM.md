# YDT 2721 卫星链路预算计算算法总结

## 1. 概述

YDT 2721 是基于中国通信行业标准 YD/T 2721-2014《地球静止卫星固定业务链路预算计算方法》的卫星链路预算软件。算法实现了从卫星参数到链路余量的完整计算流程，支持晴天和降雨两种场景的链路分析。

## 2. 模块架构

算法由六个核心模块（M02-M07）组成，主入口为 `calculator.py` 中的 `complete_link_budget()` 函数：

```
M02 (satellite.py)     → 卫星SFD、天线增益面积
M03 (carrier.py)       → 传输速率、符号率、带宽
M04 (earth_station.py) → 天线增益、指向角、G/T值
M05 (space_loss.py)    → 自由空间损耗、降雨衰减、G/T下降
M06 (clear_sky.py)     → C/N计算、干扰、系统余量
M07 (rain_impact.py)   → 上行降雨UPC补偿、下行降雨C/N
```

## 3. 核心算法

### 3.1 M02 - 卫星参数计算

**饱和通量密度 (SFD):**
```
SFD_s = SFD_ref - (G/T_s - G/T_s,ref)
```

**天线孔径单位面积增益:**
```
G_m² = 10 × lg(4π / λ²)
λ = c / f
```

### 3.2 M03 - 载波带宽计算

**传输速率:**
```
R_t = R_b / σ_FEC
```

**符号速率:**
```
R_c = R_t × M          (M = 调制阶数)
R_s = R_c / n          (n = 每符号比特数)
```

**带宽参数:**
```
BW_n = R_s × α₁        (噪声带宽)
BW_a = R_s × α₂        (分配带宽)
η_BW = (BW_a / BW_tr) × 100%
```

### 3.3 M04 - 地球站参数计算

**天线增益:**
```
G_e = 20 × lg(πD/λ) + 10 × lg(η)
```

**卫星距离:**
```
d = 42644 × √(1 - 0.2954 × cos(ψ))
cos(ψ) = cos(θ₁) × cos(φ)
```

**G/T值:**
```
T_es = T_a/L_fr + (1 - 1/L_fr) × T₀ + T_er
G/T_e = G_er - L_fr - 10 × lg(T_es)
```

### 3.4 M05 - 空间损耗计算

**自由空间损耗:**
```
L = 20 × lg(f) + 20 × lg(d) + 32.45
(f: MHz, d: km)
```

**降雨噪声温度:**
```
T_s = T_m × (1 - 1/10^(A_pd/10))
```

**G/T下降:**
```
Δ(G/T_e) = 10 × lg((T_s/L_fr + T_es) / T_es)
```

### 3.5 M06 - 晴天链路计算

**上行C/N:**
```
C/N_u = PFD_s - G_m² + G/T_s - 10×lg(BW_n) - k
```

**下行C/N:**
```
C/N_d = EIRP_sl - L_d - L_ar + G/T_e - 10×lg(BW_n) - k
```

**系统C/N (功率叠加):**
```
1/(C/N_T) = Σ 1/(C/N_i)
```

**门限C/N:**
```
C/N_th = E_b/N_o,th + 10×lg(R_b) - 10×lg(BW_n)
```

**系统余量:**
```
M = C/N_T - C/N_th
```

**卫星功率分配:**
```
EIRP_sl = EIRP_ss - BO_o + 10×lg(带宽比)
PFD = SFD - BO_i - (EIRP_ss - BO_o - EIRP_sl)
```

### 3.6 M07 - 降雨影响计算

**上行降雨 (UPC补偿):**
```
A_UPC = min(A_pu, A_UPC,max)
EIRP_sl = EIRP_ss - BO_ol - A_pu + A_UPC
```

**下行降雨C/N:**
```
C/N_d = EIRP_sl - L_d - L_ar - A_pd + G/T_e - Δ(G/T_e) - 10×lg(BW_n) - k
```

### 3.7 余量调整

通过二分查找调整卫星EIRP实现目标余量：
```
find_eirp_for_target_margin(target_margin, margin_func)
迭代搜索 EIRP ∈ [eirp_min, eirp_max]
收敛条件: |margin - target_margin| < tolerance
```

## 4. 计算流程

```
输入参数
    ↓
[1] 卫星参数计算 (M02)
    - SFD、天线增益面积
    ↓
[2] 载波带宽计算 (M03)
    - 传输速率、符号率、噪声带宽、分配带宽、带宽占用比
    ↓
[3] 地球站参数计算 (M04)
    - 发射/接收天线增益、仰角、方位角、卫星距离、G/T值
    ↓
[4] 空间损耗计算 (M05)
    - 上下行自由空间损耗
    - ITU-Rpy降雨衰减计算
    - G/T下降
    ↓
[5] 卫星功率分配 (M06)
    - 计算载波EIRP、PFD、输入/输出回退
    ↓
[6] 反向计算 (UPC和功放)
    - 从可用度计算所需UPC余量和功放功率
    ↓
[7] 晴天链路计算 (M06)
    - C/N_u, C/N_d, C/N_T
    - 各干扰项C/I
    - 系统余量
    - EIRP、发射功率、功放功率
    ↓
[8] 上行降雨计算 (M07)
    - UPC补偿量
    - 降雨余量
    ↓
[9] 下行降雨计算 (M07)
    - 降雨下行C/N
    - 降雨系统余量
    ↓
[10] 余量调整 (可选)
    - 二分搜索调整EIRP
    - 重新计算所有参数
    ↓
输出 LinkBudgetResult
```

## 5. 关键参数

### 5.1 单位制

- 内部计算使用 **Hz**
- 用户参数使用 **GHz**
- 转换: `frequency_hz = frequency_ghz × 1e9`

### 5.2 常量

- 光速: c = 299792458 m/s
- 波尔兹曼常数: k = -228.6 dBW/K
- 调制指数: BPSK=1, QPSK=2, 8PSK=3, 16QAM=4

### 5.3 降雨模型

使用 **ITU-Rpy** 库计算：
- 降雨衰减
- 气体衰减
- 云衰减
- 闪烁衰减
- 降雨噪声温度

## 6. 物理意义

| 参数 | 物理意义 |
|------|----------|
| SFD | 卫星灵敏度，表示达到饱和所需的功率通量密度 |
| G/T | 品质因数，天线增益与系统噪声温度之比 |
| C/N | 载噪比，载波功率与噪声功率之比 |
| EIRP | 等效全向辐射功率，发射机实际辐射功率 |
| UPC | 上行功率控制，补偿降雨引起的衰减 |
| 余量 | 系统储备，用于应对链路波动 |

## 7. 参考公式索引

| 公式 | 来源 |
|------|------|
| SFD_s = SFD_ref - (G/T_s - G/T_s,ref) | YDT 2721 M02 |
| G_m² = 10×lg(4π/λ²) | YDT 2721 M02 |
| L = 20×lg(f) + 20×lg(d) + 32.45 | YDT 2721 M05 |
| C/N_u = PFD - G_m² + G/T_s - 10×lg(BW_n) - k | YDT 2721 M06 |
| C/N_d = EIRP_sl - L_d - L_ar + G/T_e - 10×lg(BW_n) - k | YDT 2721 M06 |
| A_UPC = min(A_pu, A_UPC,max) | YDT 2721 M07 |